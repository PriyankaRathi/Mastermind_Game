<% if @game.guesses.count > 0 %>
  <table>
    <% @game.guesses.reverse_each do |guess| %>
      <% if guess.first_color != nil %>
        <tr>
          <td> <%= image_tag ColorScheme.find_color(guess.first_color), alt: ColorScheme.find_color(guess.first_color)%> </td>
          <td> <%= image_tag ColorScheme.find_color(guess.second_color), alt: ColorScheme.find_color(guess.second_color) %> </td>
          <td> <%= image_tag ColorScheme.find_color(guess.third_color), alt: ColorScheme.find_color(guess.third_color) %> </td>
          <td> <%= image_tag ColorScheme.find_color(guess.fourth_color), alt: ColorScheme.find_color(guess.fourth_color) %> </td>
          <td>
            <p>
              <%= pluralize_sentence(guess.find_correct_digits_count, "color_count") %>
              <br>
              <%= pluralize_sentence(guess.find_correct_at_wrong_postion_digit_count, "color_count_wrong_postion") %>
            </p> 
          </td>   
        </tr>
      <% end %>
    <% end %>
  </table>
<% end %>